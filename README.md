# Whack-a-Mole 打灰狼

中文 | [English](README-en.md)

## 项目介绍

这是一个移动端优先的 H5 打地鼠类小游戏。玩家在 30 秒内点击随机出现的角色：打中灰太狼加分，打中小灰灰扣分。项目使用静态 HTML、CSS、JavaScript、jQuery 和 Bootstrap 实现，不依赖构建工具。

![游戏截图](assets/screenshot.png)

## 功能特性

- 移动端 rem 自适应布局。
- 30 秒倒计时游戏流程。
- 9 个固定洞位随机出狼。
- 灰太狼和小灰灰两类目标随机出现。
- 点击灰太狼加 10 分，点击小灰灰扣 10 分，最低分为 0。
- 背景音乐开关。
- 击中和误点音效。
- 游戏规则弹窗。
- 排行榜弹窗结构预留。

## 目录结构

```text
.
├── index.html              # 页面结构和弹窗内容
├── css/
│   ├── style.css           # 游戏页面样式
│   └── bootstrap.min.css   # Bootstrap 样式
├── js/
│   ├── main.js             # 游戏主逻辑
│   ├── jquery-3.3.1.min.js # jQuery
│   └── bootstrap.min.js    # Bootstrap 脚本
├── image/                  # 项目运行所需图片
├── audio/                  # 背景音乐和点击音效
└── assets/                 # 文档截图、备用图片和原始切图素材
```

## 运行方式

项目是静态页面，直接在浏览器中打开 `index.html` 即可运行。

也可以使用任意静态服务器启动，例如：

```bash
python3 -m http.server 8000
```

然后访问：

```text
http://localhost:8000
```

## 核心逻辑

1. 页面加载后，`js/main.js` 根据屏幕宽度设置根字号，实现移动端适配。
2. 点击“开始游戏”后，隐藏开始面板，播放背景音乐，启动倒计时和出狼循环。
3. 出狼循环会创建一个 `img` 元素，随机选择 9 个洞位之一，并随机决定生成灰太狼或小灰灰。
4. 角色动画通过定时切换 `image/grey-wolf-0.png` 至 `image/grey-wolf-9.png` 或 `image/little-grey-0.png` 至 `image/little-grey-9.png` 实现。
5. 玩家点击角色后，同一只狼只允许计分一次。
6. 点击灰太狼时分数加 10，点击小灰灰时分数减 10。
7. 倒计时结束后停止出狼循环，展示结算面板和最终得分。
8. 点击“重新开始”会重置分数、时间和界面状态，并重新进入游戏。

## 规则说明

- 每局游戏时间为 30 秒。
- 打中灰太狼：`+10` 分。
- 打中小灰灰：`-10` 分。
- 分数不会低于 0。
